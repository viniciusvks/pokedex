const STATUS_LOADING = 0;
const STATUS_FETCHED = 1;
const STATUS_ERROR = 2;

export const status = Object.freeze({

    LOADING: STATUS_LOADING,
    FETCHED: STATUS_FETCHED,
    ERROR: STATUS_ERROR

});

export const evolutionChainType = Object.freeze({

    LINEAR: 0,
    LINEAR: 1,
    LINEAR: 2,
    MULTIPLE: 3

});